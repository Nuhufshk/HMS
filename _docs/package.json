{
  "name": "adom-hospital",
  "private": true,
  "version": "1.0.0",
  "description": "Adom Medical Centre — Hospital Management System (frontend + backend)",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "test:smoke": "node frontend/scripts/smoke.mjs",
    "test:roles": "node frontend/scripts/roles.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
